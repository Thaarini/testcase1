machine:
  ruby:
    version: rbx-2.2.6

general:
   artifacts:
     - $CIRCLE_TEST_REPORTS
     - target
     
test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - bundle exec junit --format json --out $CIRCLE_TEST_REPORTS/junit/tests.junit
